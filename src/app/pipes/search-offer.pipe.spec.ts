import { SearchOfferPipe } from './search-offer.pipe';

describe('SearchOfferPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOfferPipe();
    expect(pipe).toBeTruthy();
  });
});
